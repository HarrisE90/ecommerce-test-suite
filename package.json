{
  "name": "ecommerce-test-suite",
  "version": "1.0.0",
  "description": "A robust, production-grade e-commerce test suite (using Playwright + TypeScript) for the Practice Software Testing demo site.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/ui/",
    "test:api": "playwright test tests/api/",
    "test:integration": "playwright test tests/integration/",
    "report": "playwright show-report"
  },
  "dependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
} 